# Frontend Placeholder

This directory is reserved for the React + Vite client application.

Planned responsibilities:
- authentication UI
- protected routes
- workspace and channel navigation
- message list and composer UI
- Socket.IO client lifecycle
- PWA installability surface
- integration with shared components from `ui/`

