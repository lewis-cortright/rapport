# Server Placeholder

This directory is reserved for the Node + Express + Socket.IO server application.

Planned responsibilities:
- authentication and current-user endpoints
- workspace, channel, and message APIs
- MongoDB connection and models
- server-side authorization
- Socket.IO authentication, rooms, and broadcasts

